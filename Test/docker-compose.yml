version: '43'

services:
  mariadb:
    restart: always
    build:
      context: requirments/mariadb
      args:
        - 입력 변수
    
    networks:
      - 네트워크 이름
    hostname: mariadb
    volumes:
      - mariadb_data:/var/lib/mysql